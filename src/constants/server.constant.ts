export enum ServerEnvironment {
	PRODUCTION = 'production',
	DEVELOPMENT = 'development'
}

export enum LogLevel {
	Fatal = 'fatal',
	Error = 'error',
	Warn = 'warn',
	Info = 'info',
	Debug = 'debug',
	Trace = 'trace'
}
